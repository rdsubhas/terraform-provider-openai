---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openai_organization_spend_limit Resource - terraform-provider-openai"
subcategory: ""
description: |-
  Manages the singleton hard spend limit for the OpenAI organization. Threshold amounts are specified in cents. Only one organization hard spend limit exists; multiple resources overwrite one another.
---

# openai_organization_spend_limit (Resource)

Manages the singleton hard spend limit for the OpenAI organization. Threshold amounts are specified in cents. Only one organization hard spend limit exists; multiple resources overwrite one another.

## Example Usage

```terraform
resource "openai_organization_spend_limit" "monthly" {
  threshold_amount = 100000
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `threshold_amount` (Number) The hard spend limit in cents.

### Optional

- `currency` (String) The threshold currency. Currently only `USD` is supported.
- `interval` (String) The evaluation interval. Currently only `month` is supported.

### Read-Only

- `enforcement` (Attributes) The current enforcement state of the hard spend limit. (see [below for nested schema](#nestedatt--enforcement))
- `id` (String) The stable singleton identifier, always `organization`.
- `object` (String) The OpenAI object type.

<a id="nestedatt--enforcement"></a>
### Nested Schema for `enforcement`

Read-Only:

- `status` (String) Whether the hard spend limit is `inactive` or `enforcing`.
